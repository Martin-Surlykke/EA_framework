package com.ea_framework.Algorithms;

import com.ea_framework.Mutation.TwoOptTsp;

public class RLS_tsp {

 /*   public static int [][] run_RLS (int [][] input, double [][] distanceMatrix, int iterations) {
        int [][] current = input;
        int i = 0;
        while (i < iterations) {

            int [][] neighbour = TwoOptTsp.twoOpt(input, distanceMatrix);
            double delta = TwoOptTsp.getDelta(current, neighbour, distanceMatrix);

            if (delta < 0) {
                current = neighbour;
            }

            i++;
        }

        return current;

    } */

}
